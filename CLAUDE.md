# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# 仓库性质

中文学习笔记仓库,主题是 **AI Evaluation Engineer 学习路线**(从传统软件测试到 AI 质量工程的转型路径)。共 13 个阶段、每阶段 12 篇左右 Markdown 文章。

**当前进度快照**:

```text
阶段 01｜AI 基础
├── 01｜AI、ML、DL 到底是什么？        ← 已完成(2026-09-07)
├── 02｜Dataset 到底是什么？           ← 未开始
├── 03-12                              ← 未开始
阶段 02-13                              ← 目录尚未创建
```

仓库还在早期,绝大多数阶段目录尚未创建。接手时不要"补完所有阶段",按当前进度往下推进即可。

总路线、阶段表、能力模型在 `README.md`;阶段目录结构、各篇目录见 `01｜AI 基础/README.md`。不要在 CLAUDE.md 重复这些内容,直接读 `README.md`。

# 目录与命名约定

阶段和文章命名都使用**全角竖线 + 半角空格**作为分隔符(`｜ `),不是半角竖线 `|`,也不是全角逗号 `,`。

```text
阶段目录:01｜AI 基础/、02｜Evaluation Fundamentals/ …
文章文件:01｜AI、ML、DL 到底是什么？.md
```

- 阶段目录名 = `编号｜阶段名`,编号固定两位、补零
- 文章文件 = `阶段内编号｜文章标题.md`,标题直接用中文问句/陈述
- 中文标点用全角:问号 `？`、逗号 `、`,不用半角
- 路径含中文和全角符号,shell/工具调用要原样保留(不要转义)

# 工作目录约定

```text
_sandbox/    实验代码(每篇配套的最小代码),git 忽略 — 见 .gitignore
_notes/      长期参考资料(读后感、案例分析、复盘),git 跟踪但不每次注入上下文
README.md    主路线图(全局唯一,不要复制到阶段目录)
CLAUDE.md    Claude 实例的项目指引(本文件)
```

实验代码放 `_sandbox/phase-XX/`,按阶段分目录;每个 phase 一个 README 记录读后感和实验总结。Python 用 `uv` 管环境,不用 pip 全局装。

# 每篇文章必须包含的要素

每篇遵循同一骨架,缺一不可:

1. **What** —— 它是什么
2. **Why** —— 为什么需要它
3. **How** —— 它如何工作,如何动手实验
4. **Evidence** —— 如何证明自己的结论可信

学习路径:**理解概念 → 建立因果链 → 真实案例 → 动手实验 → 产生数据 → Evaluation → Failure Analysis → 复盘 → 形成可复用能力**。

每篇文章结尾应给"完成标准"或类似产出要求,让"看完"不等于"学完"。

# 写作风格

中文。遵循项目根 `~/.claude/CLAUDE.md` 里的写作方法论,这里只列几条最容易踩的:

- 开门见山:第一段说"读完能获得什么",不要寒暄
- 一段一观点;超过 5 行拆段
- 具体 > 抽象:举例、数字、代码(代码块标记语言);少用"非常重要""极其关键"
- 避免 AI 味词汇:"总而言之""值得注意的是""在当今时代""首先…其次…最后"
- 标题/列表/代码块打散纯文字墙
- 结尾给可执行的"下一步"(学习路径、动手实验、参考资料)

# 改前先确认

- 这是个人学习笔记,不是团队代码。改之前问自己:这是为了**改结构**还是**改内容**?
- **结构改动**(新建阶段目录、移动/重命名文章、调整路线)——先说方案再动手,不要直接 mv
- **内容改动**(措辞、补充、补图)——直接改即可
- 涉及删除旧版/旧文件前,先 `git log` 确认没在用,再删
- 中文文件名在 git、shell、IDE 之间表现不一致是常见现象,任何工具报错贴**原文 + 关键上下文**别只说"失败了"

# 不要做的事

- 不要在阶段目录名上用半角竖线 `|` 或全角逗号 `,`(会破坏命名一致性)
- 不要把已有文章改个标题就当作新文章(篇号和标题必须配得上)
- 不要跳阶段写后面的内容(README 已经规定依赖关系:01 → 02 → … → 13)
- 不要在文章里堆同义词强调、堆引用、每段都用"首先…其次"
- 不要在没有看完 `README.md` 和 `01｜AI 基础/README.md` 的情况下提"补完所有阶段"这类建议